{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Build Server",
			"command": "${workspaceFolder}/build.bat",
			"group": "build",
			"options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ]
		}
	]
}